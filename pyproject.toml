[project]
name = "dbt-test-assignment"
version = "0.1.0"
description = "Analytics engineer take-home: dbt + DuckDB pipeline on retail store transaction data"
requires-python = ">=3.12"
dependencies = [
    "dbt-duckdb>=1.10.1",
    "ipykernel>=7.3.0",
    "pandas>=3.0.3",
]
